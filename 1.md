已把 DeepTutor 的 mini-app 前端并进 health-app/miniapp-vue3 的学习模块里了，做法是保留你原有的 pages/study/index、pages/study/chat、pages/study/add，再把 DeepTutor 整套页面挂到 pages/study/deeptutor/* 下，避免把现有学习计划路由撞坏。
我这次实际完成了这几件事：
重写了 pages.json，把 DeepTutor 子页面全部注册进去，并顺手修复成可解析的干净版本。
在 pages/study/index.vue 加了一个 DeepTutor 入口，不影响你原来的学习计划卡片和跳转。
把 DeepTutor 页面复制到 health-app/miniapp-vue3/src/pages/study/deeptutor/，并把它依赖的 api/components/store/utils/static 一并放到 pages/study/ 旁边，尽量复用原始相对路径。
修掉了 DeepTutor 内部关键跳转，统一改成 /pages/study/deeptutor/...，并去掉了原先不适合你项目的 switchTab 用法。
我还做了两项校验：
pages.json 现在能成功被 PowerShell 的 ConvertFrom-Json 解析。
全文回扫后，没有再发现 DeepTutor 子模块里残留旧的 /pages/login/login、/pages/knowledge/*、switchTab(...) 这类会直接跳坏的旧路径。
还没完成的一点是构建验证。我尝试跑了 npm run build:h5，但当前环境缺少 uni 命令，所以没法在这台机器上完成打包检查。报错是：'uni' is not recognized as an internal or external command。
如果你愿意，我下一步可以继续帮你做两件事中的一个：
继续清理现在多出来但未使用的 src/deeptutor/ 副本，避免目录重复。
直接把 miniapp-vue3 的本地开发/构建环境补齐，然后跑一次真机构建验证。1